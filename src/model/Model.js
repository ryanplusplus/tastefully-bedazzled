const axios = require('axios');

export default async (key) => {
  const o = {};

  o.listNotes = async () => {

  };

  o.getNote = async (id) => {

  };

  o.addNote = async () => {

  };

  o.deleteNote = async () => {

  };

  return o;
};
